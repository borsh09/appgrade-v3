import { CartPage } from '@/components/commerce/cart-page';
export const metadata = { title: 'Корзина — APPGRADE', robots: { index: false, follow: false } };
export default function Page() { return <CartPage />; }
