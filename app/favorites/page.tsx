import { FavoritesPage } from '@/components/commerce/favorites-page';
export const metadata = { title: 'Избранное — APPGRADE', robots: { index: false, follow: false } };
export default function Page() { return <FavoritesPage />; }
